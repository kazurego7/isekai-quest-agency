name: CI

on:
  pull_request:
  push:
    branches: [main, work, develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: quest_test
          POSTGRES_PASSWORD: test-only-password
          POSTGRES_DB: isekai_quest_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U quest_test -d isekai_quest_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://quest_test:test-only-password@127.0.0.1:5432/isekai_quest_test
      TEST_DATABASE_URL: postgresql://quest_test:test-only-password@127.0.0.1:5432/isekai_quest_test
      NEXTAUTH_URL: http://127.0.0.1:3000
      NEXTAUTH_SECRET: ci-only-secret-never-use-in-production
      NEXT_TELEMETRY_DISABLED: 1
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - run: pnpm install --frozen-lockfile
      - run: pnpm audit --audit-level high
      - run: pnpm db:generate
      - run: pnpm db:push --skip-generate
      - run: pnpm lint
      - run: pnpm test:integration
      - run: pnpm build
      - run: pnpm test:http
